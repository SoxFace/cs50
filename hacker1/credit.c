#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // multiply odd # index * 2
        // start with 2nd last digit
    // add sum of individual digits
    // to sum of non-multiplied digits
    // if totals last digit is 0, the number is valid
        // if total % 0 is true
}
